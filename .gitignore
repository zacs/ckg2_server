# Backups / images produced by 00-preflight-backup.sh (or the ssh one-liner
# in the README, which writes a .img.gz) — never commit these
*.img
*.img.gz
*.img.zst
*.img*.sha256

# Python
__pycache__/
*.pyc

# Editor noise
*.swp
.DS_Store
